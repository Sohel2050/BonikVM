<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- ========================================================= -->
    <!-- Screen / Device Support                                   -->
    <!-- ========================================================= -->

    <supports-screens
        android:resizeable="true"
        android:smallScreens="true"
        android:normalScreens="true"
        android:largeScreens="true"
        android:xlargeScreens="true"
        android:anyDensity="true" />


    <!-- ========================================================= -->
    <!-- Network Permissions                                       -->
    <!-- ========================================================= -->

    <uses-permission android:name="android.permission.INTERNET" />

    <uses-permission
        android:name="android.permission.ACCESS_NETWORK_STATE" />

    <uses-permission
        android:name="android.permission.ACCESS_WIFI_STATE" />

    <uses-permission
        android:name="android.permission.CHANGE_NETWORK_STATE" />

    <uses-permission
        android:name="android.permission.CHANGE_WIFI_STATE" />

    <uses-permission
        android:name="android.permission.WAKE_LOCK" />

    <uses-permission
        android:name="android.permission.REQUEST_IGNORE_BATTERY_OPTIMIZATIONS" />


    <!-- ========================================================= -->
    <!-- VPN                                                        -->
    <!-- ========================================================= -->

    <!-- VPN service binding is declared on the service itself -->
    <!-- Do NOT add android.permission.VPN -->

    <!-- VPN binding permission (required by OpenVPN/WireGuard/V2Ray services below) -->
    <!-- [ADDED] Needed because the services declare android:permission="android.permission.BIND_VPN_SERVICE" -->
    <uses-permission android:name="android.permission.BIND_VPN_SERVICE" />


    <!-- ========================================================= -->
    <!-- Android Foreground Service                                -->
    <!-- ========================================================= -->

    <!-- Required for foreground services -->
    <uses-permission
        android:name="android.permission.FOREGROUND_SERVICE" />

    <!-- Required for specialUse foreground service on Android 14+ -->
    <uses-permission
        android:name="android.permission.FOREGROUND_SERVICE_SPECIAL_USE" />

    <!-- [ADDED] Preventively blocks this permission from being pulled in by any
         library (axevpn_flutter / wireguard-android) in future dependency updates.
         This permission is for wearable/companion-device links, NOT VPN, and its
         presence caused a Play Console policy declaration mismatch earlier. -->
    <uses-permission
        android:name="android.permission.FOREGROUND_SERVICE_CONNECTED_DEVICE"
        tools:node="remove" />


    <!-- ========================================================= -->
    <!-- Remove Location Permissions                               -->
    <!-- ========================================================= -->

    <uses-permission
        android:name="android.permission.ACCESS_FINE_LOCATION"
        tools:node="remove" />

    <uses-permission
        android:name="android.permission.ACCESS_COARSE_LOCATION"
        tools:node="remove" />

    <uses-permission
        android:name="android.permission.ACCESS_BACKGROUND_LOCATION"
        tools:node="remove" />


    <!-- ========================================================= -->
    <!-- Notification                                               -->
    <!-- ========================================================= -->

    <uses-permission
        android:name="android.permission.POST_NOTIFICATIONS" />


    <!-- ========================================================= -->
    <!-- AdMob                                                      -->
    <!-- ========================================================= -->

    <uses-permission
        android:name="com.google.android.gms.permission.AD_ID" />


    <!-- ========================================================= -->
    <!-- Google Play Billing                                        -->
    <!-- ========================================================= -->

    <uses-permission
        android:name="com.android.vending.BILLING" />


    <!-- ========================================================= -->
    <!-- Application                                                 -->
    <!-- ========================================================= -->

    <application
        android:name="${applicationName}"
        android:label="VPN MASTER"
        android:icon="@mipmap/launcher_icon"
        android:extractNativeLibs="true"
        android:networkSecurityConfig="@xml/network_security_config"
        android:usesCleartextTraffic="false">


        <!-- ===================================================== -->
        <!-- Application Metadata                                  -->
        <!-- ===================================================== -->

        <meta-data
            android:name="android.max_aspect"
            android:value="2.4" />

        <meta-data
            android:name="android.allow_multiple_resumed_activities"
            android:value="true" />

        <!-- [ADDED] Required for Android 15+ / Google Play 16 KB page size compliance -->
        <meta-data
            android:name="android.max_page_size"
            android:value="16384" />


        <!-- ===================================================== -->
        <!-- AdMob App ID                                           -->
        <!-- ===================================================== -->

        <meta-data
            android:name="com.google.android.gms.ads.APPLICATION_ID"
            android:value="ca-app-pub-9189896335015331~9603802785" />


        <!-- ===================================================== -->
        <!-- Main Flutter Activity                                 -->
        <!-- ===================================================== -->

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:taskAffinity=""
            android:theme="@style/LaunchTheme"
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|smallestScreenSize|locale|layoutDirection|fontScale|screenLayout|density|uiMode"
            android:hardwareAccelerated="true"
            android:windowSoftInputMode="adjustResize">

            <meta-data
                android:name="io.flutter.embedding.android.NormalTheme"
                android:resource="@style/NormalTheme" />

            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

        </activity>


        <!-- ===================================================== -->
        <!-- OpenVPN Activities                                    -->
        <!-- ===================================================== -->

        <activity
            android:name="de.blinkt.openvpn.LaunchVPN"
            android:exported="false"
            android:theme="@android:style/Theme.Translucent.NoTitleBar"
            android:launchMode="singleTask"
            android:excludeFromRecents="true"
            tools:replace="android:exported,android:theme" />

        <activity
            android:name="de.blinkt.openvpn.activities.DisconnectVPN"
            android:exported="false"
            android:theme="@android:style/Theme.Translucent.NoTitleBar"
            android:taskAffinity=""
            android:excludeFromRecents="true" />

        <activity
            android:name="de.blinkt.openvpn.DisconnectVPNActivity"
            android:exported="false"
            android:theme="@android:style/Theme.Translucent.NoTitleBar"
            android:taskAffinity=""
            android:excludeFromRecents="true" />


        <!-- ===================================================== -->
        <!-- OpenVPN Foreground VPN Service                        -->
        <!-- ===================================================== -->

        <!--
            Android 14+ requires a declared foregroundServiceType. There is no
            dedicated "vpn" FGS type, so specialUse is the correct type for
            third-party VPN tunnels (per Play Console policy guidance).
            tools:replace is required because axevpn_flutter's own manifest
            declares this service too (without a type, which is what caused
            the Play rejection) — this forces our specialUse declaration to win
            during manifest merging instead of being dropped or concatenated.
        -->
        <service
            android:name="de.blinkt.openvpn.core.OpenVPNService"
            android:exported="false"
            android:permission="android.permission.BIND_VPN_SERVICE"
            android:foregroundServiceType="specialUse"
            android:stopWithTask="false"
            tools:replace="android:foregroundServiceType">
            <property
                android:name="android.app.PROPERTY_SPECIAL_USE_FGS_SUBTYPE"
                android:value="vpn" />
            <intent-filter>
                <action android:name="android.net.VpnService" />
            </intent-filter>
        </service>

        <!--
            AxeWireGuardService / AxeWireGuardVpnService / AxeV2RayVpnService /
            AxeOpenConnectVpnService / GoBackend$VpnService are declared identically
            in axevpn_flutter's own manifest (specialUse + the FGS subtype
            property) and merge in automatically. Do NOT redeclare them here
            without tools:replace — doing so previously caused the manifest
            merger to concatenate the type into "specialUse|specialUse".
        -->


        <!-- ===================================================== -->
        <!-- Unity LevelPlay / IronSource                          -->
        <!-- ===================================================== -->

        <activity
            android:name="com.ironsource.mediationsdk.sdk.RewardedVideoActivity"
            android:configChanges="orientation|screenSize"
            android:hardwareAccelerated="true"
            android:theme="@android:style/Theme.Translucent" />

        <activity
            android:name="com.ironsource.mediationsdk.sdk.InterstitialActivity"
            android:configChanges="orientation|screenSize"
            android:hardwareAccelerated="true"
            android:theme="@android:style/Theme.Translucent" />

        <activity
            android:name="com.ironsource.mediationsdk.utils.IronSourceWebViewActivity"
            android:configChanges="orientation|screenSize"
            android:hardwareAccelerated="true"
            android:theme="@android:style/Theme.Translucent" />


        <!-- ===================================================== -->
        <!-- Flutter Embedding                                     -->
        <!-- ===================================================== -->

        <meta-data
            android:name="flutterEmbedding"
            android:value="2" />

    </application>


    <!-- ========================================================= -->
    <!-- Package Visibility / Queries                              -->
    <!-- ========================================================= -->

    <queries>

        <!-- Flutter Process Text -->
        <intent>
            <action android:name="android.intent.action.PROCESS_TEXT" />
            <data android:mimeType="text/plain" />
        </intent>

        <!-- Chrome Custom Tabs -->
        <intent>
            <action android:name="android.support.customtabs.action.CustomTabsService" />
        </intent>

        <!-- HTTPS -->
        <intent>
            <action android:name="android.intent.action.VIEW" />
            <category android:name="android.intent.category.BROWSABLE" />
            <data android:scheme="https" />
        </intent>

        <!-- HTTP -->
        <intent>
            <action android:name="android.intent.action.VIEW" />
            <category android:name="android.intent.category.BROWSABLE" />
            <data android:scheme="http" />
        </intent>

    </queries>

</manifest>
